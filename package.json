{
  "name": "donation-counter-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for Cash Counter / Donation Tracker",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "crypto-js": "^4.2.0"
  }
}
